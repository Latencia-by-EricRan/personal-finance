feat: add Norte design system theme (Fase 0)

Nu Purple-derived Material 3 dark ColorScheme, Manrope/Inter type
scale via google_fonts, and component themes (card, buttons, nav bar,
segmented control, bottom sheet) matching the reviewed design system.
